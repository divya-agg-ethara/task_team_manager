# Team Task Manager — client

## Environment

Create `.env.local`:

```bash
# Backend API base URL (must include /api/v1 if your server uses that prefix)
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
```
